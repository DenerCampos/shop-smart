export type ChoreRecurrence = 'once' | 'daily' | 'weekly';

export const CHORE_RECURRENCES = {
  ONCE: 'once' as ChoreRecurrence,
  DAILY: 'daily' as ChoreRecurrence,
  WEEKLY: 'weekly' as ChoreRecurrence,
};
