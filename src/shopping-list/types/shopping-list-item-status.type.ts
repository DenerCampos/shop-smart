export type ShoppingListItemStatus = 'pending' | 'in_cart';

export const SHOPPING_LIST_ITEM_STATUS = {
  PENDING: 'pending' as ShoppingListItemStatus,
  IN_CART: 'in_cart' as ShoppingListItemStatus,
};
